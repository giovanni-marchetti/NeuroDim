# NeuroDim
`PyTorch` script to estimate the dimension of the function space of a deep self-attention network. 


Run `dim_estimator.py` to to produce the plot of the estimated dimension as hidden dimension varies. The parameters (architecture, number of Monte Carlo samples, etc.) can be set at the beginning of the file.   

